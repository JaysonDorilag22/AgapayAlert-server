const NOTIFICATION_SOUNDS = {
    ALERT: {
      ios: './alert.wav', // Reference local file in constants directory
      android: 'alert'
    },
    DEFAULT: {
      ios: 'default',
      android: 'default' 
    }
  };
  
  module.exports = {
    NOTIFICATION_SOUNDS
  };